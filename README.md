# Hangman

Created this project to get experience working with curses library.

Windows users need to install `windows-curses` library to play this, all others can run it directly.

## Usage

`python ./main.py`
